
<h1>Add a comment: </h1>

<%= form_for [post, post.comments.new] do |f| %>
  
    <%= f.label :body %>
    <%= f.text_field :body %>
 

  <div class="form-group">
   <%= f.submit "Save" %>
  </div>
  
<% end %>